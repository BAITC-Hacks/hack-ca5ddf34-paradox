{
  "name": "ekt-chat-frontend",
  "version": "0.3.0",
  "private": true,
  "description": "EKT AI chat frontend with server-owned sessions and confirmed cart proposals",
  "scripts": {
    "dev": "node build.mjs && node dev-server.mjs",
    "build": "node build.mjs",
    "test": "node --test tests/*.test.cjs",
    "test:contract": "node --test tests/api.test.cjs tests/view-model.test.cjs",
    "check": "node --check api.js && node --check view-model.js && node --check saved-store.js && node --check app.js && node --check dev-server.mjs && node build.mjs --check"
  },
  "devDependencies": { "linkedom": "0.18.13" }
}
